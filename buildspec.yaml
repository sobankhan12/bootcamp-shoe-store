
version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 16

  build:
    commands:
      - echo Build started on `date`
      - echo Compiling the Node.js code
      - yarn install
  post_build:
    commands:
      - echo Build completed on `date`
      - yarn build build
      
artifacts:
  files:
    - dist/*
